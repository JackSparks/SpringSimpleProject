spring:
  application:
    name: simple-microservice  # This must match the name used by the Config Server to look for the config file (e.g., 'microservice' in the Git repo)
  cloud:
    config:
      uri: http://localhost:8888  # URI for the Config Server, default is port 8888
