spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mydb
    username: user
    password: password

  server:
    port: 8080

  cloud:
    config:
      uri: http://localhost:8888  # URI to the Config Server, it can be relative if on the same network

# Custom properties for your microservice
microservice:
  name: "My Spring Microservice"
  version: "1.0.0"
